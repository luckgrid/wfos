# WfOS project graph.
# Explicit sources while packages/apps are still being scaffolded; switch to
# `apps/*` / `packages/*` globs once each has its own moon.yml.
projects:
  sources:
    wfos: "."
    panoply: "packages/panoply"
    ontarch: "packages/ontarch"
    takogami: "packages/takogami"

vcs:
  defaultBranch: main
