# WfOS gitleaks baseline. Extends the built-in ruleset and adds a low-noise allowlist for
# generated registry output and session logs. Used by the pre-commit `gitleaks` hook
# (gitleaks git --staged) and the report-only `ontarch secrets-scan` routine.
title = "wfos gitleaks baseline"

[extend]
useDefault = true

[allowlist]
description = "generated + non-secret paths"
paths = [
  '''packages/ontarch/registry/.*\.json$''',
  '''packages/ontarch/registry/.*\.dot$''',
  '''packages/ontarch/registry/SCAN\.md$''',
  '''packages/ontarch/registry/sessions/.*\.json$''',
  '''.*\.example$''',
  '''.*\.sample$''',
]
