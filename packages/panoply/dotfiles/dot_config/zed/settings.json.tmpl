{{- /*
  ~/.config/zed/settings.json — managed by chezmoi (WfOS). Profile: {{ .profile }}.
  App-specific Zed syntax only; scope/policy/rails stay in Workstreams/.agents/profiles.
  Gates GUI-facing features on the machine profile `gui` flag (agent-safe excludes editor-gui).
  Draft (gateway_state: draft): chezmoi source, not applied to $HOME.
*/ -}}
{{- $p := index .profiles .profile -}}
{
  "//": "GENERATED by chezmoi (WfOS) for profile {{ .profile }}. Do not hand-edit.",
  "telemetry": {
    "metrics": {{ $p.gui }}
  },
  "assistant": {
    "enabled": {{ $p.gui }}
  }
}
