# sops creation rules — the `files` vault (git-checked config, values encrypted).
#
# Recipient below is a **fixture-only** age public key (generated for this repo sample).
# It is not a production secret — see README.md for the paired test private key and
# regeneration steps. Production hosts provision their own recipient (human step).
creation_rules:
  - path_regex: \.enc\.(ya?ml|json|env)$
    age: "age1sypm2y50ryrz80fq70gpavmrgp4wrscru8zz9v6yuats4rc393sq54ftfs"
