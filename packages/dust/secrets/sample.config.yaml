# Structure-only sample for the sops + age `files` vault. NOT a secret.
# This is the plaintext shape of a config whose VALUES would be sops-encrypted. The placeholder
# values below are intentionally fake; encrypt with `sops` to produce `sample.config.enc.yaml`
# (see README.md). After encryption, keys stay readable and only the values become ciphertext.
service:
  name: example
  endpoint: https://api.example.test
secrets:
  api_key: REPLACE_VIA_SOPS
  webhook_token: REPLACE_VIA_SOPS
