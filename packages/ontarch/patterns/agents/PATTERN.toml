# Agents navigation pattern — seed for a working `$AGENTS_HOME` (.agents/).
# Materialized by `ontarch agents-init`. Working copies pin this via `.pattern-lock`.
id = "agents-navigation"
version = "0.1.0"
kind = "pattern"
title = "Agent navigation layer"
purpose = "Seed profiles, skills index, tools, and graphs navigation contracts for a consuming workspace"

# Schema contracts live in the metadata-plane package (not duplicated here).
profile_schema = "schemas/profile.schema.json"
skill_schema = "schemas/skill.schema.json"
edges_schema = "graphs/edges.schema.json"
